numpy>=1.24
pandas>=2.0
pyyaml>=6.0
tqdm>=4.65
matplotlib>=3.7
tabulate>=0.9

# PyTorch 建议根据服务器 CUDA 版本单独安装，例如：
#   pip install torch torchvision torchaudio
# 下面的版本号仅供参考，请根据实际环境调整
torch>=2.0
torchvision>=0.15
torchaudio>=2.0
